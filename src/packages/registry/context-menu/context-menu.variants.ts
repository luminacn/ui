import { cva } from '../../../lib/cva';

export const contextMenuContentVariants = cva(
  'lm-context-menu z-50 min-w-[10rem] overflow-hidden rounded-md border bg-popover text-popover-foreground shadow-lg p-1',
);
